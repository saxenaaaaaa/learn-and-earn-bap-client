export interface ITrainingNetworkContext {
  transaction_id: string;
  message_id: string;
  action: string;
  timestamp: string;
  country: string;
  city: string;
  version: string;
  bap_uri: string;
  bap_id: string;
  domain: string;
  bpp_id: string;
  bpp_uri: string;
}
